#include <iostream>

using namespace std;

int main()
{
    cout << "Enter the radious of the sphere : ";
    int radius = 0;
    cin >> radius;
    cout << "Area of the sphere is : " << (4 * (22/7) * radius) << endl;
    return 0;
}